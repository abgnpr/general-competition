---
title: Percentages
parent: Quantitative Aptitude
nav_order: 10
---

# Percentages

**Tier 1 · Priority 1 · The master key.** Unlocks P&L, SI/CI, Ratios, and most DI.

Status: Day 1 complete (2026-04-20)

## Concept Map

A percentage is a ratio with base 100. Every percentage problem is a disguised multiplication. The mental model: **convert to a multiplier, then multiply.** Don't compute additively — chain multipliers.

## Formulas That Actually Matter

| # | Formula | When |
|---|---------|------|
| 1 | `x% of N = (x/100) × N` | Basic calculation |
| 2 | `% change = (new − old) / old × 100` | Any increase/decrease |
| 3 | `fraction change = (new − old) / old` | Same as above, ratio form |
| 4 | If a value changes by a% then by b%: net % change = `[(1 ± a/100)(1 ± b/100) − 1] × 100` | Exact result for any two sequential % changes. Can easily be scaled for more than two |
| 5 | Same as above, expanded shortcut: net % = `a + b + ab/100` (use sign of each; increase = +, decrease = −) | Faster for MCQs when a, b are small integers. Works only for two |
| 6 | Multiplier form: increase r% → `×(1 + r/100)`; decrease r% → `×(1 − r/100)` | Convert % to multiplier |
| 7 | Base change (more→less): if A is r% more than B → B is `r/(100+r) × 100`% less than A | Comparing two different bases |
| 8 | Base change (less→more): if A is r% less than B → B is `r/(100−r) × 100`% more than A | Reverse base change |
| 9 | `X% of Y = Y% of X` | Swap trick for fast mental math |
| 10 | `% of whole = (part/whole) × 100` | Finding what % a part is |
| 11 | `part = (% / 100) × whole` | Finding the actual value |
| 12 | `whole = part × (100 / %)` | Finding original when part+% known |

## Conversion Table (memorize cold)

| Fraction | % |
|----------|---|
| 1/2 | 50% |
| 1/3 | 33.33% |
| 1/4 | 25% |
| 1/5 | 20% |
| 1/6 | 16.67% |
| 1/7 | 14.29% |
| 1/8 | 12.5% |
| 1/9 | 11.11% |
| 1/10 | 10% |
| 1/11 | 9.09% |
| 1/12 | 8.33% |
| 1/15 | 6.67% |
| 1/20 | 5% |

## Mental Models / Shortcuts

- `20% increase = ×1.2`, `15% decrease = ×0.85` — always a multiplier, never additive
- Successive change: 1.2 × 0.9 = 1.08 → net **+8%** (not +10%)
- Successive shortcut `a + b + ab/100`: +20% then −10% → 20 − 10 + (20×−10)/100 = 10 − 2 = **+8%** (same answer, faster for MCQs)
- Base change (more→less): r% more → `r/(100+r)`% less. E.g., 25% more → 25/125 = **20% less**
- Base change (less→more): r% less → `r/(100−r)`% more. E.g., 20% less → 20/80 = **25% more**
- `X% of Y = Y% of X` — swap when one side is easier (e.g., 5% of 80 = 80% of 5 = 4)
- To find original before a % change: `original = final / multiplier` (e.g., after +25%, original = final / 1.25)

## Common Traps

- Mixing up "% of" vs "% more than" vs "% less than" — base shifts each time
- Successive changes: adding percentages (wrong) instead of multiplying factors (right)
- Base change: "A is 25% more than B" ≠ "B is 25% less than A" — base is different
- Forgetting to subtract 1 after multiplying factors to get net % change
- Applying additive logic where multiplicative is needed (population, interest, DI growth)

## Problem Patterns

- Income/expenditure → translate each to `base × multiplier`
- Population growth → successive multiplication chain
- Exam marks → identify base before computing "% more/less"
- Salary change → chain multipliers, read net from final factor
- "What % of X is Y?" → `(Y/X) × 100`

## DI Integration

- Pie charts: all values are percentages of a whole — base is total
- Tables with % growth: successive multiplier chain across years
- Bar/Line with % change column: `% change = (new−old)/old × 100`

## Revision Triggers

- Convert 1/7, 1/9, 1/11 to % instantly
- A is 40% more than B → B is what % less than A? (answer: 28.57%)
- Salary up 30%, down 20% → net? (answer: +4%)
- 15% of 240 using swap trick
